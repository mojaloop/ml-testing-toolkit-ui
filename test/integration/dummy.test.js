// Dummy integration test to ensure CI pipelines pass
import { describe, it, expect } from 'vitest'

describe('Dummy integration test', () => {
  it('should pass', () => {
    expect(true).toBe(true)
  })
}) 