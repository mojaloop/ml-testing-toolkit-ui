## Reject only dependency upgrades that are empirically broken here; document the concrete failure (exact error/version) next to each entry.
reject:
  # @types/node's major must match the Node runtime major in .nvmrc (currently 24). A higher major
  # makes the TS toolchain accept Node-26-only APIs that then crash at runtime on Node 24 — a failure
  # mode the test gates cannot catch (it only weakens type checking). Bump together with the runtime.
  - '@types/node'
