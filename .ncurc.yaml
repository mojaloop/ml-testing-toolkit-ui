## Add a TODO comment indicating the reason for each rejected dependency upgrade added to this list, and what should be done to resolve it (i.e. handle it through a story, etc).
reject: [
    "@types/jest",
    "typescript", # conflicts with typescript-eslint
    "eslint", # eslint 10.x not yet supported by typescript-eslint 8.x
    "@eslint/js", # must stay in sync with eslint version
    "jsdom" # jsdom 26+ requires Node >= 22.12.0; CI orb uses 22.11.0
]
